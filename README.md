# Flip-Coin-Dice-and-Random-Number-
App made to generate random flip coins, dice rolls and random rumbers between a set interval generated in a backend server.

Start the app with node app.js in the terminal
